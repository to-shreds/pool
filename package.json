{
  "name": "cue-lab",
  "version": "0.2.0",
  "private": true,
  "description": "Offline browser pool with a three-dimensional ball-state simulation and local rulesets",
  "scripts": {"test": "node --test tests/*.test.js", "build": "python3 build.py"},
  "engines": {"node": ">=18"}
}
